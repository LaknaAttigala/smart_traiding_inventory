<flectra>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="smart_traiding_inventory.list">
      <field name="name">smart_traiding_inventory list</field>
      <field name="model">smart_traiding_inventory.smart_traiding_inventory</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="smart_traiding_inventory.action_window">
      <field name="name">smart_traiding_inventory window</field>
      <field name="res_model">smart_traiding_inventory.smart_traiding_inventory</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="smart_traiding_inventory.action_server">
      <field name="name">smart_traiding_inventory server</field>
      <field name="model_id" ref="model_smart_traiding_inventory_smart_traiding_inventory"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="smart_traiding_inventory" id="smart_traiding_inventory.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="smart_traiding_inventory.menu_1" parent="smart_traiding_inventory.menu_root"/>
    <menuitem name="Menu 2" id="smart_traiding_inventory.menu_2" parent="smart_traiding_inventory.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="smart_traiding_inventory.menu_1_list" parent="smart_traiding_inventory.menu_1"
              action="smart_traiding_inventory.action_window"/>
    <menuitem name="Server to list" id="smart_traiding_inventory" parent="smart_traiding_inventory.menu_2"
              action="smart_traiding_inventory.action_server"/>
    -->
  </data>
</flectra>